/**
 * Modulo per compilare progetti LaTeX in PDF con parametri per file.
 * La struttura dei parametri ricalca la struttura cartelle/file del progetto.
 */
module.exports = require('./compileLatex');
