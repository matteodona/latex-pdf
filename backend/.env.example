<<<<<<< HEAD
# Impostazioni consigliate per produzione (Hostinger VPS)
DJANGO_SETTINGS_MODULE=config.settings.production
DJANGO_SECRET_KEY=imposta-una-chiave-lunga-e-casuale
DJANGO_ALLOWED_HOSTS=api.tuodominio.it

# Origine frontend usata per CORS
FRONTEND_URL=https://www.tuodominio.it
# Facoltativo: lista esplicita separata da virgole
# CORS_ALLOWED_ORIGINS=https://www.tuodominio.it,https://app.tuodominio.it

# PostgreSQL (opzione A): URL completo
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require

# PostgreSQL (opzione B): variabili separate
POSTGRES_DB=cardy
POSTGRES_USER=cardy
POSTGRES_PASSWORD=change-me
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_SSLMODE=require
POSTGRES_CONN_MAX_AGE=60

# Hardening HTTP
DJANGO_SECURE_SSL_REDIRECT=true
DJANGO_SECURE_HSTS_SECONDS=31536000
DJANGO_SECURE_HSTS_INCLUDE_SUBDOMAINS=true
DJANGO_SECURE_HSTS_PRELOAD=true
=======
# Copia in .env sul server (non committare .env).
# Django
DJANGO_SECRET_KEY=genera-una-chiave-lunga-e-casuale
DJANGO_ALLOWED_HOSTS=tuodominio.it,www.tuodominio.it
FRONTEND_URL=https://tuodominio.it
CORS_ALLOWED_ORIGINS=https://tuodominio.it

# PostgreSQL — opzione A: URL unico (tipico su PaaS)
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/NOMEDB?sslmode=require

# PostgreSQL — opzione B: variabili separate
POSTGRES_DB=cardy
POSTGRES_USER=cardy
POSTGRES_PASSWORD=
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_SSLMODE=prefer
POSTGRES_CONN_MAX_AGE=60
>>>>>>> abbe939 (Refactor project structure to transition from Node/Express to Django for backend, implement PostgreSQL support, and enhance template management with a new API. Update README for clarity and add environment configuration examples. Introduce new frontend features for template selection and PDF generation.)
